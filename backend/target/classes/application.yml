server:
  port: 9090

# Actuator configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

# Set default profile to dev for development
spring:
  profiles:
    active: dev